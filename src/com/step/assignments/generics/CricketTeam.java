package com.step.assignments.generics;

public class CricketTeam extends Team {
    public CricketTeam(String teamName, int wonPoints, int lostPoints, int drawPoints) {
        super(teamName, wonPoints, lostPoints, drawPoints);
    }
}
